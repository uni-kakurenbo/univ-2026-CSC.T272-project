{
    "name": "@app/runner",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "bun build --target bun --outfile dist/main.js src/main.ts",
        "start": "bun run src/main.ts",
        "test": "bun test src",
        "typecheck": "tsc --noEmit -p tsconfig.json"
    },
    "dependencies": {
        "@app/shared": "workspace:*",
        "hono": "4.12.27"
    },
    "devDependencies": {
        "@types/bun": "1.3.14",
        "typescript": "6.0.3"
    }
}
